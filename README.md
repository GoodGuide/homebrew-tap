# goodguide/tap Homebrew Tap

This is a Homebrew Tap for GG-specific projects that have no value in the main
homebrew codebase.

"Tap" into it (only need to do this once) via:

```bash
homebrew tap goodguide/tap
```
